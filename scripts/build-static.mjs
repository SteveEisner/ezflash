import {cp,mkdir,readFile,writeFile} from 'node:fs/promises'; import {resolve} from 'node:path';
const root=resolve('.'),release=process.env.EASY_FLASH_RELEASE||'v0',out=resolve(root,'dist'); await mkdir(resolve(out,'releases',release,'firmware'),{recursive:true});
for(const f of ['index.html','styles.css','firmware-bench.css','app.mjs','local-flash.mjs','profiles.mjs','firmware-ui.mjs','device-identity.mjs','safety-contract.mjs','operation-receipts.mjs']) await cp(resolve(root,'easy-flash',f),resolve(out,f)); await cp(resolve(root,'easy-flash/vendor'),resolve(out,'vendor'),{recursive:true});
const src=JSON.parse(await readFile(resolve(root,'easy-flash/firmware-manifest.json'),'utf8')); for(const a of src.variants[0].artifacts){const n=a.path.split('/').pop(); await cp(resolve(root,'easy-flash',a.path),resolve(out,'releases',release,'firmware',n)); a.path=`firmware/${n}`;} await writeFile(resolve(out,'releases',release,'manifest.json'),JSON.stringify(src,null,2)+'\n'); await writeFile(resolve(out,'current.json'),JSON.stringify({releaseId:release})+'\n'); await cp(resolve(root,'_headers'),resolve(out,'_headers')); console.log(`built ${out}`);
